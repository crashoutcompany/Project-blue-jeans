{
  "name": "project-blue-jeans",
  "install": "npm ci && npx playwright install --with-deps chromium",
  "terminals": [
    {
      "name": "next-dev",
      "command": ": \"${NEON_AUTH_BASE_URL:=https://example.invalid}\"; : \"${NEON_AUTH_COOKIE_SECRET:=01234567890123456789012345678901}\"; export NEON_AUTH_BASE_URL NEON_AUTH_COOKIE_SECRET; npm run dev"
    }
  ]
}
